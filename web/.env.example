# Salin ke .env.local lalu sesuaikan.
# URL dasar API (termasuk prefix /api/v1). WAJIB di-set untuk build produksi:
# build akan gagal bila variabel ini kosong.
NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1
