import 'package:flutter/material.dart';

/// Palet warna dasar aplikasi Toko CV IndoMurah Shop.
/// Ganti sesuai brand guideline resmi toko jika sudah ada.
class AppColors {
  AppColors._();

  static const Color primary = Color(0xFFE53935); // merah — identik toko diskon/murah
  static const Color primaryDark = Color(0xFFB71C1C);
  static const Color secondary = Color(0xFFFFA000); // aksen kuning/orange (harga, promo)
  static const Color background = Color(0xFFF7F7F7);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF6B6B6B);
  static const Color success = Color(0xFF2E7D32);
  static const Color error = Color(0xFFD32F2F);
  static const Color border = Color(0xFFE0E0E0);
}
