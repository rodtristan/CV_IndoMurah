# Environment
.env
.env.local
.env.*.local

# Dependencies
node_modules/
package-lock.json

# Prisma
# NOTE: prisma/migrations/ is intentionally NOT ignored — those .sql files
# must be committed so `prisma migrate deploy` (run on every boot, see
# Dockerfile) has something to apply on Railway/production.
/generated/prisma

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Build
dist/
build/
*.tsbuildinfo
*.dump
